## 1. 传输事件与能力探针

- [x] 1.1 采集并脱敏当前支持的 Claude CLI `stream-json` 样本，覆盖工具调用、可关联结果、重试、任务状态、压缩通知、错误和中断，并固定为解析夹具。
- [x] 1.2 扩展传输事件类型和解析器，保留已证实的活动关联标识与结构化状态，声明基础活动、结果、进度和通知能力，并安全忽略未知字段。
- [x] 1.3 为每次发送生成并传递回合标识，由主进程为归一化事件附加递增顺序号后再转发给渲染层。

## 2. 回合记录与会话兼容

- [x] 2.1 定义版本化输出记录、活动详情、系统通知和终止状态的共享类型，并保留旧消息的兼容投影类型。
- [x] 2.2 重构回合组装器，使其基于带顺序号的事件包产生稳定的追加、更新、会话更新和实时视图提交，并正确处理结果迟到、失败和中断。
- [x] 2.3 扩展会话 JSON 读写：旧消息可惰性转换为只读历史记录，首次新版写入原子保存规范记录与兼容消息投影。
- [x] 2.4 更新主进程和渲染层状态消费逻辑，使它们应用同一带稳定标识的提交序列，并避免按文本增量重复落盘。

## 3. 工具活动详情与安全处理

- [x] 3.1 实现工具活动摘要映射，覆盖文件读取、搜索、编辑、命令执行和未知工具，且只使用传输中可确定的信息。
- [x] 3.2 在持久化前实现原始输入、输出和差异详情的每活动与每回合大小限制、凭据脱敏、截断标记和可复制文本处理。
- [x] 3.3 为基础能力、结果不可用、工具失败和回合中断建立一致的活动生命周期显示数据，确保不推测完成状态或进度。

## 4. 输出工作区界面

- [x] 4.1 以回合输出视图替换平铺工具消息渲染，保留助手 Markdown、代码高亮和复制操作，并按实际顺序展示正文、活动和通知。
- [x] 4.2 实现连续活动分组、摘要、默认展开规则、单项详情展开和失败或中断状态，并提供键盘可访问的展开控制。
- [x] 4.3 接入活动与状态的中英文客户端文案，确保 Claude 正文、工具名、路径、命令和原始内容不被翻译。
- [x] 4.4 实现接近底部时的流式跟随、离开底部后的未读输出提示和跳转到最新输出操作。

## 5. 回归验证

- [x] 5.1 为解析器、回合组装器和主进程/渲染层提交一致性补充夹具测试，覆盖丰富能力与降级能力、错误、终止和顺序稳定性。
- [x] 5.2 为会话迁移、兼容消息投影、原子写入失败、输出截断和凭据脱敏增加存储与安全测试。
- [x] 5.3 为输出视图补充渲染与交互测试，覆盖活动分组、详情展开、语言切换、代码复制、阅读位置和两种主题。
- [x] 5.4 运行测试、类型检查、生产构建和 OpenSpec 严格校验，修复本变更引入的问题并记录验证结果。
