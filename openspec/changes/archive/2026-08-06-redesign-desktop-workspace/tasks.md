## 1. 外观偏好基础

- [x] 1.1 定义外观偏好与有效主题类型，在主进程实现独立、原子写入的本地偏好存储；缺失或无效值回退为浅色。
- [x] 1.2 在主进程注册最小化的外观偏好 IPC，并在“跟随系统”模式下订阅和推送系统外观变化。
- [x] 1.3 扩展 preload、IPC 封装与 Zustand 状态，使渲染层初始化偏好、更新偏好并将有效主题设置到根元素。
- [x] 1.4 为偏好存储与有效主题计算添加单元测试，覆盖默认浅色、手动偏好恢复和跟随系统变化。

## 2. 主题令牌与通用视觉组件

- [x] 2.1 重构 `App.css` 为浅色默认和深色覆盖的语义化令牌，定义表面、文本、边框、焦点、状态和代码块颜色。
- [x] 2.2 将现有输入、按钮、弹窗、消息、工具调用、错误和中断样式迁移为 className 与语义令牌，移除阻碍主题切换的内联色值。
- [x] 2.3 实现随有效主题切换的 Markdown 高亮、代码块和复制按钮样式，并验证浅色与深色下的对比度。
- [x] 2.4 引入并按需使用统一图标库，替换导航和工具操作中的文字或 emoji 图标；为图标按钮提供 tooltip 和可访问名称。

## 3. 工作台与会话导航

- [x] 3.1 重构应用壳层和侧栏，提供产品入口、新建会话、最近会话、激活态和底部设置入口，同时保留会话 CRUD 行为。
- [x] 3.2 实现锚定式设置菜单，提供浅色、深色和跟随系统的三态选择、当前状态标识以及点击外部或 Escape 关闭行为。
- [x] 3.3 重构聊天视图为会话上下文条、受限阅读宽度的消息列和独立贴底组合器，保持消息滚动、流式更新和停止操作正常。
- [x] 3.4 将组合器草稿改为由聊天工作区控制，添加空会话欢迎区和任务建议卡片；选择建议时填入并聚焦输入框且不自动发送。
- [x] 3.5 调整模型选择、权限状态、删除确认和 CLI 错误提示的布局，使它们在两种主题与最小桌面窗口尺寸下都不遮挡主要操作。

## 4. 验证与文档

- [x] 4.1 运行并补充渲染层相关测试，覆盖主题切换时保留会话、消息、生成态和任务建议草稿行为。
- [x] 4.2 在 Electron 中分别验证浅色、深色和跟随系统模式，包括重启恢复与运行中系统主题切换。
- [x] 4.3 对空会话、长消息会话、工具调用、代码块、错误、危险放行确认和生成中状态进行浅色/深色视觉回归，检查 1280px 与最小桌面宽度下的布局。
- [x] 4.4 运行 `npm run typecheck` 与 `npm test`，更新 `docs/design.md` 和验证矩阵以记录主题与工作台的实现及验证结果。

## 5. 默认主题与输入区细化

- [x] 5.1 将缺失或无效外观偏好的默认值调整为“跟随系统”，并更新主进程存储测试与设计文档。
- [x] 5.2 细化组合器的编辑与操作层级，保持受控草稿、自动增高、Enter 发送、Shift+Enter 换行和停止生成行为，并为视觉状态补充测试或可验证的覆盖。
- [x] 5.3 运行类型检查、单元测试和 Electron 预览，验证首次主题回退、手动主题恢复以及输入栏的空、聚焦、多行和生成中状态。

## 6. Claude Code 空会话建议

- [x] 6.1 将空会话任务建议改为“探索当前项目”“规划一次改动”“定位并修复问题”“审查当前工作区”，为每项提供可编辑的 Claude Code 工作流草稿与紧凑说明。
- [x] 6.2 补充空会话建议的渲染覆盖，并在 Electron 中验证四项建议填入草稿、不自动发送，以及浅色和深色下的信息密度与布局。
