## 1. 会话授权数据与 CLI 参数

- [x] 1.1 在共享会话类型中定义可执行的授权模式、可读选项、默认值与兼容规范化逻辑。
- [x] 1.2 扩展会话存储及索引，使授权模式可持久化并让旧会话回退到默认值。
- [x] 1.3 收紧会话更新与发送 IPC，使主进程仅以保存的会话授权模式启动 CLI，并移除全局临时放行状态。

## 2. 组合器控制与输入状态

- [x] 2.1 更新渲染层会话状态操作，使模型和授权模式均能按会话保存并在生成期间锁定。
- [x] 2.2 替换危险放行开关为带全部放行确认的授权模式选择控件，并增强模型选择项的产品名和 CLI 别名显示。
- [x] 2.3 根据项目目录、CLI 可用性和生成状态更新输入提示、发送条件及组合器上下文布局。

## 3. 回归验证

- [x] 3.1 增加或更新单元测试，覆盖授权模式迁移、会话级 IPC 权威值、控件锁定和输入状态提示。
- [x] 3.2 运行测试、类型检查、生产构建与 OpenSpec 严格校验，并修复发现的问题。
